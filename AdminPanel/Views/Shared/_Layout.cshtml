@inject IConfiguration Configuration
<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - AdminPanel</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
<header class="site-header">
    <div class="header-left">Atlas Library</div>

    <nav class="top-nav">
        <a asp-controller="Admin" asp-action="Index">Panel</a>
        <a href="@(Configuration["ExternalLinks:ManageObjectsUrl"] ?? "#")">Hantera Objekt</a>
        <a href="@(Configuration["ExternalLinks:ManageReservationsUrl"] ?? "#")">Hantera Reservation</a>
    </nav>

    <div class="header-right">
        <a href="/Profile" class="top-action-btn profile-btn">Profil</a>
        <a href="/Login" class="top-action-btn sign-in-btn">Logga In</a>
    </div>
</header>

<main class="page-wrapper">
    @RenderBody()
</main>

@await RenderSectionAsync("Scripts", required: false)
</body>
</html>